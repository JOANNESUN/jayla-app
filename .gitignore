# Xcode user state (window layout, breakpoints, schemes) — per-machine noise
xcuserdata/
*.xcuserstate

# Build artifacts
DerivedData/
build/

# macOS
.DS_Store

# Claude Code local state (lives inside the synchronized folder for now)
Jayla/.claude/
